import { FileText } from 'lucide-react';
import { StubPage } from '@/components/layout/StubPage';

export default function BriefsPage() {
  return (
    <StubPage
      title="Briefs"
      description="AI-generated strategic briefs — why a trend moves, why it matters, and where the whitespace is."
      icon={FileText}
    />
  );
}
