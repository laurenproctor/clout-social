# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — keep .env.example committed, never the real secrets
.env
.env*.local

# local-dev media uploads (fallback when Vercel Blob isn't configured)
/public/uploads

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env*

# ...but always keep the secret-free template tracked
!.env.example
